{
  "name": "example",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && electron ./dist/main.js"
  },
  "devDependencies": {
    "electron": "^24.3.0",
    "typescript": "^4.8.3"
  }
}
